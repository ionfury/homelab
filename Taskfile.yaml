---
version: "3"

includes:
  inventory: .taskfiles/inventory
  workstation: .taskfiles/workstation
  terragrunt: .taskfiles/terragrunt
  kubernetes: .taskfiles/kubernetes

tasks:
  default:
    silent: true
    cmds: ["task -l"]
