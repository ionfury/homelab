version: "3"

tasks:
  finalize-pending-volume-attachments:
    desc: Remove the finalizers on VolumeAttachments pending deletion to remediate docs/runbooks/unstick-volume-attachments.md.
    cmds:
      - echo "Script this next time it happens lol"
