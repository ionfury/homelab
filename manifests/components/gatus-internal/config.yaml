endpoints:
  - name: "${HOSTNAME}"
    group: internal
    url: "https://${HOSTNAME}.${INTERNAL_DOMAIN}"
    interval: 1m
    client:
      dns-resolver: "udp://${DNS_IP}:53"
    conditions:
      - "[STATUS] == 200"
