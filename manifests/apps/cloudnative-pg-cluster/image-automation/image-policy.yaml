apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: automation
spec:
  filterTags:
    pattern: '^(?P<major>\d+)\.(?P<minor>\d+)-(?P<revision>\d+)$'
    extract: '$major.$minor.$revision'
  policy:
    semver:
      range: '15.x' # Pins to major version 15
