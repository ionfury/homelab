resources:
  - redis-cluster.yaml
secretGenerator:
  - name: ${NAME}-redis-auth
    options:
      disableNameSuffixHash: true
      annotations:
        secret-generator.v1.mittwald.de/autogenerate: password
