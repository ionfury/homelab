namePrefix: robusta-
resources:
  - external-secret.yaml
  - rules
components:
  - ../../components/helm-release
configMapGenerator:
  - name: values
    behavior: replace
    files:
      - values.yaml