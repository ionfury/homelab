controllers:
  main:
    annotations:
      reloader.stakater.com/auto: "true"
    containers:
      main:
        env:
          JF_URL: http://jellyfin-app.${NAMESPACE}.svc.cluster.local/
          TOKEN:
            valueFrom:
              secretKeyRef:
                name: jellyfin-exporter-token
                key: token
defaultPodOptions:
  automountServiceAccountToken: false
  enableServiceLinks: false
service:
  main:
    ports:
      http:
        port: 8080
serviceMonitor:
  main:
    enabled: true
    endpoints:
      - port: http
        scheme: http
        path: /metrics
        interval: 1m
        scrapeTimeout: 10s
