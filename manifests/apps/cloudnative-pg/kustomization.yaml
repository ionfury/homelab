namePrefix: cloudnative-pg-
components:
  - ../../components/helm-release
resources:
  - prometheus-rule.yaml
configMapGenerator:
  - name: values
    behavior: replace
    files:
      - values.yaml
