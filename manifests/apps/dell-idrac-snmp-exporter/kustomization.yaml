namePrefix: dell-idrac-snmp-exporter-
components:
  - ../.templates/helm-release
configMapGenerator:
  - name: values
    behavior: replace
    files:
      - values.yaml
  - name: config
    options:
      disableNameSuffixHash: true
    files:
      - snmp.yaml
