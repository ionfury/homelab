namePrefix: transmission-
components:
  - ../../components/helm-release-app-template-oci
resources:
  - external-secrets.yaml
configMapGenerator:
  - name: values
    behavior: replace
    files:
      - values.yaml
