namePrefix: cloudflare-dyndns-
components:
  - ../.templates/helm-release
resources:
  - external-secret.yaml
configMapGenerator:
  - name: values
    behavior: replace
    files:
      - values.yaml
