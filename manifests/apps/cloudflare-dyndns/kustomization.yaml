namePrefix: cloudflare-dyndns-
components:
  - ../../components/helm-release
resources:
  - external-secret.yaml
configMapGenerator:
  - name: values
    behavior: replace
    files:
      - values.yaml
