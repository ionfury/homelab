namePrefix: rancher-backup-
components:
  - ../../components/helm-release
resources:
  - backup.yaml
  - external-secret.yaml
configMapGenerator:
  - name: values
    behavior: replace
    files:
      - values.yaml
