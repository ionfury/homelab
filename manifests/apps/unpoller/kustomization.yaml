namePrefix: unpoller-
components:
  - ../.templates/helm-release-app-template
resources:
  - external-secret.yaml
configMapGenerator:
  - name: values
    behavior: replace
    files:
      - values.yaml
