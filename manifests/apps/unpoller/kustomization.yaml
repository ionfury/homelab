namePrefix: unpoller-
components:
  - ../../components/helm-release-app-template-oci
resources:
  - external-secret.yaml
configMapGenerator:
  - name: values
    behavior: replace
    files:
      - values.yaml
