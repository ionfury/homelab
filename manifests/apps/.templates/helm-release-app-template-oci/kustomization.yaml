apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
components:
  - ../../../components/configurations/helm-release
  - ../../../components/configurations/image-repository.yaml
  - ../../../components/configurations/image-policy.yaml
configurations:
  - configuration.yaml
resources:
  - helm-release.yaml
  - image-policy.yaml
  - image-repository.yaml
configMapGenerator:
  - name: values
    literals:
      - values.yaml=
secretGenerator:
  - name: secret-values
    literals:
      - values.yaml=
