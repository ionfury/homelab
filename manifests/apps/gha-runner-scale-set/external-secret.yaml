apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: auth
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-ssm
  target:
    name: gha-runner-auth
  dataFrom:
    - key: k8s-gha-runners
