components:
  - ../../../components/configurations/cluster-issuer

resources:
  - cluster-issuer.yaml

secretGenerator:
  - name: cloudflare-api-token-secret
    files:
      - api-token=values/cloudflare-api-token.encrypted