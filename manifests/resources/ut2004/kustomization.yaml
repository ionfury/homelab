namespace: ut2004

components:
  - ../../../manifests/components/namespace

resources:
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: ut
  app.kubernetes.io/version: "2004"
  app.kubernetes.io/component: server

configMapGenerator:
  - name: ut2004-env
    envs: [values.env]
    behavior: create