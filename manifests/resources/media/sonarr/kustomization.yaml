namePrefix: sonarr-
components:
  - ../../../components/helm-app-template
resources:
  - image-automation.yaml
configMapGenerator:
  - name: values
    behavior: replace
    files:
      - values.yaml