namespace: valheim

components:
  - ../../components/configurations/helm-release
  - ../../../manifests/components/flux-sops-key
  - ../../../manifests/components/flux-ssh-key

resources:
  - sync.yaml
  - helm-release.yaml
  - helm-repository.yaml
  - namespace.yaml

configMapGenerator:
  - name: valheim-values
    files:
      - values.yaml

secretGenerator:
  - name: valheim-secret-values
    files:
      - values.yaml=secret-values.encrypted.yaml