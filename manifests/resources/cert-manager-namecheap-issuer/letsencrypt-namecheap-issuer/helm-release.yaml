apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: letsencrypt-namecheap-issuer
spec:
  interval: 10m
  dependsOn:
    - name: cert-manager
    - name: namecheap-webhook
  chart:
    spec:
      chart: letsencrypt-namecheap-issuer
      version: 0.1.0
      sourceRef:
        kind: HelmRepository
        name: letsencrypt-namecheap-issuer
      interval: 10m
  valuesFrom:
    - kind: ConfigMap
      name: letsencrypt-namecheap-issuer-values