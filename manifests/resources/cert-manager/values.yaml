global:
  leaderElection:
    namespace: "cert-manager"
    
cert-manager:
  deploymentAnnotations:
    certmanager.k8s.io/disable-validation: "true"

namespace: cert-manager

installCRDs: true

prometheus:
  enabled: false

startupapicheck:
  enabled: false