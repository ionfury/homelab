namespace: project-zomboid

components:
  - ../../../manifests/components/namespace

resources:
  - deployment.yaml
  - service.yaml
  - persistent-volume-claim.yaml

images:
  - name: image
    newName: danixu86/project-zomboid-dedicated-server
    newTag: 41.78.16

commonLabels:
  app.kubernetes.io/name: project-zomboid
  app.kubernetes.io/component: server

secretGenerator:
  - name: project-zomboid-env
    envs: [values.encrypted.env]
    behavior: create