apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ingress-nginx
spec:
  path: ./manifests/apps/ingress-nginx
  postBuild:
    substitute:
      HELM_CHART: ingress-nginx
      HELM_CHART_VERSION: 4.4.2 # {"$imagepolicy": "network:ingress-nginx-automation:tag"}
      HELM_CHART_REPOSITORY: ghcr.io/nginxinc/charts
