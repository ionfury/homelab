apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: dell-idrac-snmp-exporter
spec:
  path: ./manifests/apps/dell-idrac-snmp-exporter
  postBuild:
    substitute:
      HELM_CHART: prometheus-snmp-exporter
      HELM_CHART_VERSION: 1.8.1 # {"$imagepolicy": "monitoring:dell-idrac-snmp-exporter-automation:tag"}
      HELM_CHART_REPOSITORY: ghcr.io/prometheus-community/charts
