apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: code-server
spec:
  path: ./manifests/apps/code-server
  postBuild:
    substitute:
      REPOSITORY: codercom/code-server
      TAG: 4.16.1 # {"$imagepolicy": "tools:code-server-automation:tag"}
