apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: not-used
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-key
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-vars
