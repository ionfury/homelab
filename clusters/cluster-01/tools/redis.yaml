apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: redis
spec:
  path: ./manifests/apps/redis-operator-cluster
  postBuild:
    substitute:
      REPOSITORY: redis
      TAG: 7.0
