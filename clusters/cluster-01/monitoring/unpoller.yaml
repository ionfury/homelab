apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: unpoller
spec:
  interval: 10m
  path: ./manifests/apps/unpoller
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  postBuild:
    substitute:
      CHART: unpoller
      VERSION: 1.5.0
