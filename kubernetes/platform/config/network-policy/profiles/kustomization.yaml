---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # NOTE: No default-deny.yaml needed - baselines with endpointSelector: {} enable
  # Cilium enforcement mode for ALL pods, providing implicit deny for unallowed traffic
  - profile-isolated.yaml
  - profile-internal.yaml
  - profile-standard.yaml
  - profile-internal-egress.yaml
