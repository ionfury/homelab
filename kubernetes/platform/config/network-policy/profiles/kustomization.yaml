---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # TODO: default-deny and profile-isolated require redesign for Cilium 1.18+
  # Empty ingress/egress arrays are no longer valid - need explicit deny rules
  # - default-deny.yaml
  # - profile-isolated.yaml
  - profile-internal.yaml
  - profile-standard.yaml
  - profile-internal-egress.yaml
