---
apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-deployment-defaults
data:
  deployment: |
    spec:
      replicas: ${default_replica_count}
  podDisruptionBudget: |
    spec:
      maxUnavailable: 1
