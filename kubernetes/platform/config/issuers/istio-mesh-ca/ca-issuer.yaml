---
# CA issuer for Istio mesh certificates.
# istio-csr requests workload certificates from this issuer.
# Uses the root CA generated by the self-signed issuer.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: istio-mesh-ca
spec:
  ca:
    secretName: istio-mesh-root-ca
