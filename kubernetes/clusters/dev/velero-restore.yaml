---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: velero-restore
  namespace: flux-system
spec:
  dependsOn:
    - name: velero-config
    - name: cilium
  interval: 10m
  path: kubernetes/clusters/dev/config/velero-restore
  force: true
  prune: true
  healthChecks:
    - apiVersion: batch/v1
      kind: Job
      name: velero-restore-gate
      namespace: velero
    - apiVersion: velero.io/v1
      kind: Restore
      name: restore-platform
      namespace: velero
  healthCheckExprs:
    - apiVersion: velero.io/v1
      kind: Restore
      current: "has(self.status) && has(self.status.phase) && self.status.phase == 'Completed'"
      failed: "has(self.status) && has(self.status.phase) && (self.status.phase == 'Failed' || self.status.phase == 'PartiallyFailed' || self.status.phase == 'FailedValidation')"
      inProgress: "!has(self.status) || !has(self.status.phase) || self.status.phase in ['New','InProgress','WaitingForPluginOperations','WaitingForPluginOperationsPartiallyFailed','Finalizing','FinalizingPartiallyFailed']"
  timeout: 30m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-vars
        optional: false
      - kind: ConfigMap
        name: platform-versions
        optional: true
