---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
# Pulls SMTP relay credentials from AWS SSM — same account used by Authelia
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: papra-smtp-credentials
  namespace: papra
spec:
  refreshInterval: 24h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-ssm
  target:
    name: papra-smtp-credentials
  data:
    - secretKey: username
      remoteRef:
        key: /homelab/kubernetes/live/authelia-smtp-credentials
        property: username
    - secretKey: password
      remoteRef:
        key: /homelab/kubernetes/live/authelia-smtp-credentials
        property: password
    - secretKey: sender
      remoteRef:
        key: /homelab/kubernetes/live/authelia-smtp-credentials
        property: sender
