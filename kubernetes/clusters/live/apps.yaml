---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-apps
  namespace: flux-system
spec:
  dependsOn:
    - name: platform
  interval: 3m
  path: kubernetes/clusters/live/apps
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-vars
        optional: false
  prune: true
  sourceRef:
    kind: OCIRepository
    name: flux-system
    namespace: flux-system
