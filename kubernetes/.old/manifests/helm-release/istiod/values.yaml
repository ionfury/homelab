profile: ambient

gobal:
  logAsJson: true
  defaultResources:
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
  multiCluster:
    enabled: true
    clusterName: "${cluster_name}"
