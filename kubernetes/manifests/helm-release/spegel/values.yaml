# https://github.com/spegel-org/spegel/blob/main/charts/spegel/values.yaml
---
spegel:
  appendMirrors: true
  containerdSock: /run/containerd/containerd.sock
  containerdRegistryConfigPath: /etc/cri/conf.d/machines
service:
  registry:
    machinePort: 29999
serviceMonitor:
  enabled: true
grafanaDashboard:
  enabled: true
