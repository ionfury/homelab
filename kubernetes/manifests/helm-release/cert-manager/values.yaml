global:
  leaderElection:
    namespace: "${NAMESPACE:kube-system}"
namespace: "${NAMESPACE:kube-system}"
networkPolicy:
  enabled: true
installCRDs: true
prometheus:
  enabled: true
  servicemonitor:
    enabled: true
