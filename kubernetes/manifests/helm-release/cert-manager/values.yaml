global:
  leaderElection:
    namespace: "${NAMESPACE:kube-system}"
namespace: "${NAMESPACE:kube-system}"
installCRDs: true
prometheus:
  enabled: true
  servicemonitor:
    enabled: true
webhook:
  networkPolicy:
    enabled: true
