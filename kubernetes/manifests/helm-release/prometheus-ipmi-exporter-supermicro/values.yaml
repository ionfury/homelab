additionalAnnotation:
  reloader.stakater.com/auto: "true"
serviceMonitor:
  enabled: false
modules:
  supermicro:
    privilege: user
    collectors:
      - bmc
      - ipmi
      - chassis
      - dcmi
      - sel
      - sm-lan-mode
    driver: LAN_2_0
    timeout: 10000
